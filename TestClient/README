This directory contains the different test clients for LatencyMeasure.

Note: ncurses and FLTK test clients are capable to be build statically.

To build dynamically:

	./build.sh

To build statically:

	./build.sh --enable-static
